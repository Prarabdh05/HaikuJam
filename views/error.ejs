<% layout('layouts/boilerplate')%>
    <div class="row">
        <div class="col-6 offset-3">
            <div class="alert alert-danger" role="alert">
                <h4 class="alert-heading">
                    <% if(err.message.search('ObjectId')) {%>
                        Page Not Found !
                        <% }else {%>
                            <%=err.message%>
                                <% } %>
                </h4>
            </div>
        </div>
    </div>